<% trips.group_by{|x| x.trip}.each_with_index do |(trip, schedule), index| %>
  <% if index < 5 %>
    <%=render partial:"welcome/next_trip/line", locals: { trip: trip, schedule: schedule } %>
  <% end %>
<% end %>