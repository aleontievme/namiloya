<%= bootstrap_form_for(@order) do |f| %>
  <%= f.hidden_field :schedule_id, value: @schedule.id %>
  <%= f.number_field :quantity, value: 1, label: "Quantity" %>
  <%= f.text_area :comment, placeholder: "Write some comments" %>
  <%= f.submit "Order" %>
<% end %>