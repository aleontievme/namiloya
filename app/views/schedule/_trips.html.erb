<%-# list of trpis and schedules for specified category -%>
<% trips.group_by{|x| x.trip}.each do |trip, schedule| %>
  <%=render partial:"welcome/next_trip/line", locals: { trip: trip, schedule: schedule } %>
<% end %>