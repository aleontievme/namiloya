<tr>
  <td>
    <a href="/trip/<%=trip.permalink%>"><%=trip.name%></a>
    <% schedule.each do |schedule| %>
      <a href="/trip/<%=trip.permalink%>/<%=schedule.begin_date%>">
      	<%= render partial: "trip/schedule_badge", locals: {schedule: schedule} %>&nbsp;
      </a>
    <% end %>
  </td>
</tr>